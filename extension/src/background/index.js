// Background Service Worker
chrome.runtime.onInstalled.addListener(() => {
    console.log('Movie Chatbot Extension Installed');
});

// We can add listeners here to help with page navigation events if needed
